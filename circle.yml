machine:
  timezone:
    Europe/Amsterdam
  environment:
    POMS_KEY: key
    POMS_ORIGIN: origin
    POMS_SECRET: secret
test:
  override:
    - bundle exec rubocop
    - bundle exec reek
    - bundle exec rspec
